# Canonical Sphinx theme and extension.
# Provides the Furo-based Canonical theme and auto-loads the common
# documentation extensions listed below when they are installed.
canonical-sphinx~=0.6

# Extensions loaded by canonical-sphinx (installed explicitly).
myst-parser~=4.0
sphinx-copybutton
sphinx-design
sphinx-notfound-page
sphinxcontrib-jquery
sphinxext-opengraph
sphinx-autobuild

# Ubuntu Pro Client-specific extensions.
sphinxcontrib-mermaid
sphinxext-rediraffe

# PDF output (SVG-to-PDF conversion for the LaTeX/PDF builder).
sphinxcontrib-svg2pdfconverter[CairoSVG]

# Spell checking.
pyspelling
