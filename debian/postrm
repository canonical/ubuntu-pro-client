#!/bin/sh

set -e


remove_cache_dir(){
    if [ -d "/var/lib/ubuntu-advantage" ]; then
        rm -rf /var/lib/ubuntu-advantage
    fi
}

case "$1" in
    purge)
        remove_cache_dir()
        ;;
esac

#DEBHELPER#

exit 0
