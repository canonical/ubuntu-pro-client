name: ubuntu-pro-client
base: ubuntu@24.04
sdks:
  - name: uv
    channel: latest/stable

actions:
  test: uv run tox -e test "$@"
  lint: uv run tox -e flake8
  typecheck: uv run tox -e mypy
  format: uv run tox -e black
