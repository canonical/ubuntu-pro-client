language: python

matrix:
  include:
    - os: linux
      dist: precise
      python: 3.4
    - os: linux
      dist: trusty
      python: 3.5
    - os: linux
      dist: trusty
      python: 3.6
      
install: pip install tox
script: make test lint
addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck
